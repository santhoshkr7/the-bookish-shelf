import {useNavigate} from 'react-router-dom';import ProgressBar from './ProgressBar.jsx';import {imageUrl} from '../services/bookService.js';
export default function BookCard({book}){const nav=useNavigate();return <article className="card" onClick={()=>nav(`/books/${book._id}`)}><div className="cover">{book.coverImage?<img src={imageUrl(book.coverImage)} onError={e=>e.currentTarget.style.display='none'}/>:<><span>📖</span><small>No cover</small></>}</div><div><h3>{book.title}</h3><p>{book.author}</p><span className="badge">{book.status}</span><ProgressBar current={book.currentPage} total={book.totalPages}/></div></article>}
