import {useEffect,useState} from 'react';import {getBooks} from '../services/bookService.js';import BookCard from '../components/BookCard.jsx';import LoadingSpinner from '../components/LoadingSpinner.jsx';import EmptyState from '../components/EmptyState.jsx';
export default function Books(){const [books,setBooks]=useState([]),[q,setQ]=useState(''),[status,setStatus]=useState(''),[genre,setGenre]=useState(''),[sort,setSort]=useState('recent'),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{setLoading(true);getBooks({search:q,status,genre,sort}).then(setBooks).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[q,status,genre,sort]);return <><div className="page-head"><div><h1>My Books</h1><p>Manage your collection</p></div></div><div className="toolbar"><input placeholder="Search title or author" value={q} onChange={e=>setQ(e.target.value)}/><select value={status} onChange={e=>setStatus(e.target.value)}><option value="">All statuses</option><option>Want to Read</option><option>Currently Reading</option><option>Finished</option></select><input placeholder="Filter genre" value={genre} onChange={e=>setGenre(e.target.value)}/><select value={sort} onChange={e=>setSort(e.target.value)}><option value="recent">Recently Added</option><option value="title">Title A-Z</option><option value="rating">Rating</option><option value="progress">Reading Progress</option></select></div>{loading?<LoadingSpinner/>:error?<p className="error">{error}</p>:books.length?<div className="grid">{books.map(b=><BookCard key={b._id} book={b}/>)}</div>:<EmptyState text="No books match your search."/>}</>}
