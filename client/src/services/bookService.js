const BASE=import.meta.env.VITE_API_URL||'http://localhost:5000/api';
async function request(path,options={}){const r=await fetch(`${BASE}${path}`,options);const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.message||'Request failed');return data}
export const getBooks=(p={})=>request('/books?'+new URLSearchParams(p));export const getBookById=id=>request(`/books/${id}`);export const getStats=()=>request('/books/stats');export const createBook=data=>request('/books',{method:'POST',body:data});export const updateBook=(id,data)=>request(`/books/${id}`,{method:'PUT',body:data});export const deleteBook=id=>request(`/books/${id}`,{method:'DELETE'});export const updateProgress=(id,currentPage)=>request(`/books/${id}/progress`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({currentPage})});export const imageUrl=p=>p?(p.startsWith('http')?p:`${BASE.replace('/api','')}${p}`):'';
